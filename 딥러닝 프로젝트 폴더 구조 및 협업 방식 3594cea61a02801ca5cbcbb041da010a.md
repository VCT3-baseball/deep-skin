# 딥러닝 프로젝트 폴더 구조 및 협업 방식

## 1. 기본 방향

이번 프로젝트는 **얼굴 이미지 데이터를 활용하여 피부 상태를 분석하고, 결과에 따라 화장품 카테고리 또는 성분을 추천하는 딥러닝 프로젝트**로 진행한다.

데이터셋은 이미지 파일 기반이기 때문에 용량이 크고, 얼굴 이미지 특성상 개인정보 이슈가 있을 수 있다.

따라서 GitHub에는 실제 데이터 파일을 업로드하지 않고, 코드와 실행 방법만 공유한다.

```
1. GitHub에는 실제 이미지 데이터 업로드 X
2. 데이터 폴더 구조만 .gitkeep으로 유지
3. 팀원들은 README를 참고해 각자 데이터셋 다운로드
4. 원본 이미지는 data/raw에 저장
5. 얼굴 부위 crop 결과는 data/cropped에 저장
6. 학습용 최종 데이터는 data/processed에 저장
7. 학습된 모델 파일은 checkpoints에 저장하되 GitHub 업로드 X
8. 최종 서비스는 Gradio 또는 Streamlit으로 구현
```

## 핵심 원칙

```
GitHub에는 코드, 노트북, 실행 방법만 올리고,
실제 데이터와 모델 가중치 파일은 올리지 않는다.
```

# 2. 추천 폴더 구조

```jsx
📦 face_skin_project
├── 📁 data/                         # 실제 데이터 저장 폴더, GitHub 업로드 X
│   ├── 📁 raw/                      # 원본 얼굴 이미지 데이터
│   │   └── 📄 .gitkeep
│   ├── 📁 cropped/                  # 얼굴 부위별 crop 결과
│   │   └── 📄 .gitkeep
│   └── 📁 processed/                # 학습용 최종 데이터
│       └── 📄 .gitkeep
│
├── 📁 notebooks/                    # 팀원별 실험용 주피터 노트북
│   ├── 📁 jh/
│   ├── 📁 jm/
│   ├── 📁 dy/
│   └── 📁 sh/
│
├── 📁 src/                          # 프로젝트 핵심 코드
│   ├── 🐍 dataset.py                # Dataset, DataLoader 생성 코드
│   ├── 🐍 crop.py                   # 얼굴 부위 crop 코드
│   ├── 🐍 model.py                  # CNN 또는 전이학습 모델 정의
│   ├── 🐍 train.py                  # 모델 학습 코드
│   ├── 🐍 predict.py                # 예측 및 추론 코드
│   ├── 📁 utils/                    # 공통 유틸리티 모듈
│   │   ├── 🐍 paths.py              # 프로젝트 경로 관리
│   │   ├── 🐍 config.py             # 설정값 관리
│   │   └── 🐍 seed.py               # random seed 고정
│   │
│   └── 📁 app/                      # Gradio 또는 Streamlit 서비스 코드
│       └── 🐍 app.py                # 웹 서비스 실행 파일
│
├── 📁 checkpoints/                  # 학습된 모델 가중치 저장, GitHub 업로드 X
│   └── 📄 .gitkeep
│
├── 📁 results/                      # 결과 이미지, 성능표, 예측 결과 저장
│   └── 📄 .gitkeep
│
├── 📄 .gitignore
├── 📄 README.md
└── 📄 requirements.txt
```

---

# 3. 폴더별 역할

## `data`

실제 이미지 데이터를 저장하는 폴더다.

얼굴 이미지 데이터는 용량이 크고 개인정보 이슈가 있을 수 있으므로 GitHub에 업로드하지 않는다.

```
data/raw        → 원본 데이터셋
data/cropped    → 얼굴 부위별 crop 결과
data/processed  → 학습용 최종 데이터
```

---

## `data/raw`

원본 얼굴 이미지 데이터셋을 저장한다.

예를 들어 AI-Hub 또는 Kaggle에서 받은 원본 이미지를 이 폴더에 저장한다.

```
data/raw/
└── dataset_name/
    ├── images/
    ├── labels/
    └── metadata.json
```

원본 데이터는 나중에 문제가 생겼을 때 다시 확인할 수 있어야 하므로 가능하면 수정하지 않고 보관한다.

---

## `data/cropped`

얼굴 부위별로 crop한 이미지를 저장한다.

예를 들어 눈가, 이마, 볼, 코, 입 주변 등으로 나누어 저장할 수 있다.

```
data/cropped/
├── forehead/
├── eyes/
├── nose/
├── cheek/
└── mouth/
```

이 폴더도 이미지 파일이 많아질 수 있으므로 GitHub에 업로드하지 않는다.

---

## `data/processed`

모델 학습에 바로 사용할 수 있도록 정리된 최종 데이터를 저장한다.

예를 들어 train, validation, test 폴더로 나누어 저장할 수 있다.

```
data/processed/
├── train/
│   ├── dry/
│   ├── oily/
│   └── normal/
│
├── val/
│   ├── dry/
│   ├── oily/
│   └── normal/
│
└── test/
    ├── dry/
    ├── oily/
    └── normal/
```

---

# 4. `notebooks` 폴더

팀원별 실험 노트북을 저장하는 공간이다.

각자 담당한 얼굴 부위 crop, EDA, 모델 실험, 학습 결과 확인 등을 진행할 수 있다.

```
notebooks/
├── jh/
├── jm/
├── dy/
└── sh/
```

예시 파일명:

```
01_data_check.ipynb
02_face_crop_test.ipynb
03_train_resnet.ipynb
04_result_check.ipynb
```

개인 실험은 자유롭게 진행하되, 최종 코드로 사용할 부분은 `src` 폴더에 정리한다.

---

# 5. `src` 폴더

→ 프로젝트 핵심 코드를 저장하는 폴더이다.

데이터 불러오기, 얼굴 부위 crop, 모델 정의, 학습, 예측, 서비스 실행 코드를 관리한다.

```
src/
├── dataset.py              # Dataset, DataLoader 생성 코드
├── crop.py                 # 얼굴 부위 crop 코드
├── model.py                # CNN 또는 전이학습 모델 정의
├── train.py                # 모델 학습 코드
├── predict.py              # 예측 및 추론 코드
│
├── utils/                  # 공통 유틸리티 모듈
│   ├── __init__.py         # utils 폴더를 패키지로 인식시키는 파일
│   ├── paths.py            # 프로젝트 경로 관리
│   ├── config.py           # batch_size, image_size 등 설정값 관리
│   └── seed.py             # random seed 고정
│
└── app/                    # Gradio 또는 Streamlit 서비스 코드
    └── app.py              # 웹 서비스 실행 파일
```

---

## `src/dataset.py`

이미지 데이터셋을 불러오는 코드를 작성한다.

주요 역할:

```
- 이미지 경로 읽기
- 라벨 정보 불러오기
- PyTorch Dataset 클래스 정의
- DataLoader 생성
- train/val/test 데이터 분리
```

---

## `src/crop.py`

얼굴 부위를 crop하는 코드를 작성한다.

주요 역할:

```
- 얼굴 영역 탐지
- 이마, 볼, 코, 입 주변 등 부위별 crop
- crop된 이미지 저장
- crop 실패 이미지 관리
```

---

## `src/model.py`

딥러닝 모델 구조를 정의한다.

주요 역할:

```
- 직접 만든 CNN 모델 정의
- ResNet, EfficientNet, ViT 등 전이학습 모델 불러오기
- 마지막 classification layer 수정
- 학습할 클래스 수 설정
```

---

## `src/train.py`

모델 학습 코드를 작성한다.

주요 역할:

```
- 학습 루프 작성
- loss 계산
- optimizer 적용
- validation loss 확인
- best model 저장
```

---

## `src/predict.py`

학습된 모델을 이용해 예측하는 코드를 작성한다.

주요 역할:

```
- 저장된 모델 불러오기
- 입력 이미지 전처리
- 피부 상태 예측
- 예측 결과 반환
- 추천 카테고리 또는 성분 연결
```

---

## `src/utils.py`

간단한 공통 함수를 모아두는 파일이다.

예시:

```
- 이미지 저장 함수
- 라벨 변환 함수
- 정확도 계산 함수
- 파일명 정리 함수
```

---

## `src/app/`

최종 서비스 코드를 저장하는 폴더다.

Gradio 또는 Streamlit을 사용하여 사용자가 얼굴 이미지를 업로드하면, 학습된 모델을 통해 피부 상태를 분석하고 추천 결과를 보여주는 화면을 구현한다.

```
src/app/
└── app.py
```

주요 역할:

```
- 이미지 업로드 화면 구성
- 업로드된 얼굴 이미지 입력
- 얼굴 부위 crop 코드 호출
- 학습된 모델 예측 코드 호출
- 피부 상태 예측 결과 출력
- 화장품 카테고리 또는 성분 추천 결과 출력
```

---

## `src/utils/`

공통 유틸리티 코드를 조금 더 분리해서 관리하는 폴더다.

→ 여러 파일에서 공통으로 사용하는 함수나 설정을 모아두는 폴더이다.

경로 관리, 설정값 관리, random seed 고정과 같은 기능을 분리해서 관리한다.

```
src/utils/
├── paths.py
├── config.py
└── seed.py
```

| 파일 | 역할 |
| --- | --- |
| `paths.py` | data, checkpoints, results 등 프로젝트 경로 관리 |
| `config.py` | batch_size, image_size, learning_rate 등 설정값 관리 |
| `seed.py` | random seed 고정으로 실험 재현성 확보 |

---

# 6. `checkpoints` 폴더

학습된 모델 가중치를 저장한다.

예시:

```
checkpoints/
├── best_model.pt
├── resnet50_best.pth
└── efficientnet_best.pth
```

모델 파일은 용량이 크기 때문에 GitHub에 업로드하지 않는다.

필요한 경우 Google Drive, Hugging Face Hub, S3 등에 업로드한 뒤 README에 링크만 공유한다.

---

# 7. `results` 폴더

학습 결과와 예측 결과를 저장한다.

예시:

```
results/
├── confusion_matrix.png
├── accuracy_graph.png
├── loss_graph.png
└── sample_predictions/
```

결과 파일도 용량이 커질 수 있으므로 기본적으로 GitHub에 업로드하지 않는다.

다만 발표에 필요한 작은 이미지 파일은 선택적으로 업로드할 수 있다.

---

---

# 8. `.gitignore` 설정

```
# data 파일은 업로드하지 않음
data/raw/*
data/cropped/*
data/processed/*

# 단, 폴더 유지용 .gitkeep은 업로드
!data/raw/.gitkeep
!data/cropped/.gitkeep
!data/processed/.gitkeep

# 모델 가중치는 업로드하지 않음
checkpoints/*
!checkpoints/.gitkeep

# 결과 파일은 기본적으로 업로드하지 않음
results/*
!results/.gitkeep

# 발표용 결과 이미지를 올리고 싶으면 아래처럼 예외 처리 가능
# !results/figures/
# !results/figures/*.png

# python cache
__pycache__/
.ipynb_checkpoints/

# virtual environment
.venv/

# OS
.DS_Store
Thumbs.db
```

---

# 09. GitHub 업로드 기준

## GitHub에 올리는 것

```
src/
notebooks/
app/
README.md
requirements.txt
.gitignore
data/raw/.gitkeep
data/cropped/.gitkeep
data/processed/.gitkeep
checkpoints/.gitkeep
results/.gitkeep
```

## GitHub에 올리지 않는 것

```
실제 얼굴 이미지 데이터
crop된 얼굴 부위 이미지
전처리된 학습용 이미지
학습된 모델 가중치 파일
대용량 결과 파일
개인 가상환경 폴더
```

---

# 10. 협업 방식

## 기본 원칙

팀원들은 같은 데이터셋을 사용하되, 실제 데이터 파일은 각자 로컬 환경에 저장한다.

```
공통으로 맞출 것:
- 데이터셋 다운로드 위치
- data 폴더 구조
- 이미지 크기
- train/val/test 분할 기준
- 라벨 기준
- 평가 지표
- 모델 저장 방식
```

---

## 팀원별 실험 방식

```
1. 각자 data/raw에 데이터셋 저장
2. notebooks에서 crop 및 모델 실험
3. 잘 동작하는 코드는 src로 이동
4. 학습된 모델은 checkpoints에 저장
5. 결과 이미지는 results에 저장
6. 최종 서비스 코드는 app에 연결
```

---

# 11. 발표용 정리 문장

```
본 프로젝트는 얼굴 이미지 데이터를 활용하여 피부 상태를 분석하고,
분석 결과에 따라 화장품 카테고리 또는 성분을 추천하는 딥러닝 기반 서비스이다.

이미지 데이터는 용량이 크고 개인정보 이슈가 있을 수 있으므로
GitHub에는 업로드하지 않고, 각 팀원이 동일한 폴더 구조에 직접 다운로드하여 사용한다.

원본 데이터는 data/raw, 얼굴 부위 crop 결과는 data/cropped,
학습용 최종 데이터는 data/processed에 저장한다.

모델 학습 및 예측 코드는 src에 정리하고,
학습된 모델 가중치는 checkpoints에 저장하되 GitHub에는 업로드하지 않는다.

최종적으로 Gradio 또는 Streamlit을 활용해
사용자가 얼굴 이미지를 업로드하면 피부 상태를 분석하고
추천 결과를 확인할 수 있는 서비스로 구현한다.
```

---

# 13. 한 줄 결론

```
딥러닝 프로젝트는 GitHub에 데이터와 모델 파일을 올리지 않고,
코드와 실행 방법만 공유한 뒤,
팀원들이 같은 폴더 구조에서 각자 데이터를 받아 학습하는 방식이 가장 적합하다.
```